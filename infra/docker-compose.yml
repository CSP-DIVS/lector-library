services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: VaKeSh#0#1
      MYSQL_DATABASE: LibraryManagement
    ports:
      - "3306:3306"
    volumes:
      - dbdata:/var/lib/mysql
volumes:
  dbdata:
